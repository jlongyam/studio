# app path url

Summary
- scheme `app://path/to/file.html` is fake location
- point location is to `manifest.json` directory
- or maybe not work in legacy version
- `inject_script` has no effect
- `nw:blank` not work



